# Desarrollo local (npm run dev): apunta al backend compartido del equipo.
# Todos usan la misma IP para compartir la base de datos SQLite del servidor.
VITE_API_URL=http://100.70.124.108:8000

# Produccion en Docker (puerto 3000): el Dockerfile usa VITE_API_URL=/api
# y nginx hace proxy a backend:8000 — no cambiar aqui para dev.
